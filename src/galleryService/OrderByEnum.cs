namespace Studio.Gallery
{
    public enum OrderByEnum
    {
        Ranking,
        Rating,
        LastModified,
        DownloadCount,
        Name,
        Author
    }

    public enum OrderByDirection
    {
        Asc,
        Desc
    }
}